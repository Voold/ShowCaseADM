import styles from './QuickActions.module.css'

export const QuickActions = () => {
  return(
      <section className={styles.mainContainer}>
          <h5 className={styles.title}>Быстрые действия</h5>
      </section>
  )
}